name: GTT Alert Daily

on:
  schedule:
    - cron: '03 15 * * 1-5'   # 8:33 AM IST = 3:03 UTC, Mon-Fri only
  workflow_dispatch:

jobs:
  run-gtt-alert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run GTT Alert
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}
          GCP_CREDENTIALS: ${{ secrets.GCP_CREDENTIALS }}
        run: python gtt_alert.py

      - name: Save JSON (commit back)
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add gtt_stocks.json
          git diff --staged --quiet || git commit -m "GTT stocks update $(date +'%Y-%m-%d')"
          git push
